# Implementation Plan: Inkwell MVP

**Branch**: `001-inkwell-mvp` | **Date**: 2026-04-10 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `specs/001-inkwell-mvp/spec.md`

---

## Summary

Inkwell is a mobile-first PWA that captures handwritten journal pages via the browser camera, runs formatting-aware OCR through a Claude vision proxy, stores entries as structured Notion pages with auto-generated tags and topical relation links, and maintains a personalized on-device calibration profile that improves OCR accuracy over time. The MVP delivers all six spec user stories: camera capture, formatting OCR, calibration onboarding, Notion publishing, auto-tagging, and entry-to-entry relations.

---

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode)
**Primary Dependencies**: React 19, Vite 6 + vite-plugin-pwa, Dexie.js v4, @notionhq/client, Tailwind CSS v4
**AI Provider**: Anthropic `claude-opus-4-6` vision via serverless proxy (`/api/ocr`, `/api/tag`, `/api/link-entries`)
**Storage**: IndexedDB (Dexie.js) for CalibrationProfile + PendingEntry; Notion API for all entry content
**Testing**: Vitest + React Testing Library (unit/component); Playwright (E2E for auth + Notion publish flows)
**Target Platform**: PWA — iOS 16+ Safari, Android 12+ Chrome; desktop Chrome/Edge secondary
**Performance Goals**: Capture-to-publish < 90s end-to-end (SC-001); OCR response < 10s p95; Notion page creation < 3s p95
**Constraints**: HTTPS required (camera API); Notion API rate limit 3 req/s; image payload to OCR proxy ≤ 5MB; iOS Safari has no `ImageCapture` API (fallback to `<input capture>`)
**Scale/Scope**: Single-user, single-device MVP; Notion corpus up to ~200 entries before entry-linking optimization is needed; no concurrent users

---

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked post-design below.*

| Principle | Status | Evidence |
|---|---|---|
| I. Formatting Semantics Are the Product | ✅ PASS | OCR output contract has typed `formatting[]` and `doodles[]` with bounding boxes. `symbols[]` reserved (FR-028). Doodle crops embedded as Notion image blocks (not described as text). |
| II. Calibration is First-Class | ✅ PASS | `CalibrationProfile` is a named IndexedDB entity. Onboarding gated behind auth. Corrections feed back via `CalibrationHints` in OCR prompt. Stored on-device (IndexedDB), no server sync. |
| III. Notion is the Database | ✅ PASS | No proprietary content database. `PendingEntry` is temporary pre-publish state, deleted after successful publish. Doodle images in Vercel Blob are media assets (not entry content). Stateless proxy holds no user data. |
| IV. Every Entry Feeds the Knowledge Graph | ✅ PASS | Tagging and entry linking are synchronous steps in the publish flow — not background jobs. Failures surface to the user (FR-024 atomicity). |
| V. PWA-Only Distribution | ✅ PASS | Vite + vite-plugin-pwa. No native wrapper. Camera via `<input capture>` on iOS, `getUserMedia` on Android/desktop. Serverless functions are not a native layer. |
| VI. MVP Scope is Non-Negotiable | ✅ PASS | Tech stack covers exactly the 6 MVP capabilities. `symbols[]` is reserved but empty — zero implementation work for Editorial Marks Intelligence in this phase. |

**Post-design re-check**: All gates continue to pass. No violations requiring Complexity Tracking entries.

---

## Project Structure

### Documentation (this feature)

```text
specs/001-inkwell-mvp/
├── plan.md              ← this file
├── research.md          ← Phase 0 decisions
├── data-model.md        ← entities and relationships
├── quickstart.md        ← 4 integration scenarios
├── contracts/
│   ├── ocr-api.md       ← POST /api/ocr + POST /api/upload-doodle
│   ├── notion-schema.md ← Notion DB schema + block structure + tagging/linking
│   └── auth.md          ← Notion OAuth flow + env vars
└── tasks.md             ← generated by /speckit.tasks (not yet created)
```

### Source Code (repository root)

```text
src/                              # PWA frontend (React + TypeScript)
├── components/
│   ├── capture/
│   │   ├── CameraCapture.tsx     # getUserMedia / <input capture> adaptive capture UI
│   │   └── OcrReviewScreen.tsx   # Diff-style correction UI, inline editing
│   ├── calibration/
│   │   ├── OnboardingFlow.tsx    # 3–5 sample collection stepper
│   │   └── SampleCapture.tsx    # Per-sample capture + preview
│   ├── auth/
│   │   └── NotionConnect.tsx     # OAuth initiation screen
│   └── common/
│       ├── ErrorBoundary.tsx
│       └── PendingEntryBanner.tsx # "N entries waiting to publish" recovery UI
├── pages/
│   ├── index.tsx                 # Root: auth guard → redirect
│   ├── onboarding/
│   │   ├── auth.tsx              # Notion OAuth entry point
│   │   └── calibration.tsx       # Calibration onboarding host
│   ├── capture.tsx               # Main capture screen
│   └── review.tsx                # OCR review + publish trigger
├── services/
│   ├── ocr.ts                    # POST /api/ocr wrapper; injects calibrationHints (profile + corrections)
│   ├── calibration.ts            # CalibrationProfile CRUD + hints serialization + analysis call
│   ├── notion.ts                 # Notion page creation; block assembly; DB bootstrap
│   ├── tagging.ts                # POST /api/tag wrapper
│   └── linking.ts                # POST /api/link-entries wrapper + Notion page fetch
├── store/
│   ├── db.ts                     # Dexie schema + versioned migrations
│   ├── calibration.ts            # CalibrationProfile / CalibrationSample / Correction ops
│   ├── pending.ts                # PendingEntry CRUD + status transitions
│   └── auth.ts                   # NotionAuthToken storage
├── lib/
│   ├── camera.ts                 # getUserMedia / input-capture detection + abstraction
│   ├── image.ts                  # Canvas-based image compression before OCR upload
│   └── auth.ts                   # OAuth initiation + hash-fragment token parsing + client-side CSRF validation
└── types/
    └── index.ts                  # OCROutput, FormattingAnnotation, DoodleRegion,
                                  # EditorialSymbol, CalibrationProfile, PendingEntry,
                                  # NotionAuthToken — all shared types

api/                              # Vercel Edge Functions (serverless proxy)
├── ocr.ts                        # POST /api/ocr → Anthropic Messages API
├── analyze-handwriting.ts        # POST /api/analyze-handwriting → multi-image analysis (onboarding only)
├── tag.ts                        # POST /api/tag → Claude tag extraction prompt
├── link-entries.ts               # POST /api/link-entries → Claude similarity prompt
└── auth/
    ├── callback.ts               # GET /api/auth/callback → Notion token exchange
    └── refresh.ts                # GET /api/auth/refresh (stubbed, Notion tokens don't expire)

public/
├── manifest.json                 # PWA manifest (name, icons, display: standalone)
└── icons/                        # PWA icon set (192×192, 512×512, maskable)

tests/
├── unit/
│   ├── services/                 # ocr.ts, calibration.ts, notion.ts unit tests
│   └── lib/                     # camera.ts, image.ts, auth.ts unit tests
├── integration/
│   └── publish-flow.test.ts     # Scenario 1–4 from quickstart.md (mocked Notion API)
└── e2e/
    ├── auth.spec.ts              # OAuth flow E2E (Playwright)
    └── publish.spec.ts           # Capture → review → publish E2E (Playwright + Notion sandbox)
```

**Structure Decision**: Web application layout with co-located Vercel Edge Functions in `/api/`. This is the standard Vite + Vercel project structure. The frontend (`src/`) and serverless functions (`api/`) share TypeScript types via `src/types/index.ts`. The serverless functions are stateless proxies only — no database, no session storage.

---

## Pre-Implementation Setup

Manual steps required before writing any code. These are one-time account and credential tasks that cannot be automated.

### 1. Anthropic API Key

- Create account at [console.anthropic.com](https://console.anthropic.com)
- Generate an API key under API Keys
- Store as `ANTHROPIC_API_KEY` in Vercel env vars (server-only) and `.env.local` for local dev
- **Cost**: Pay-per-token. A full capture-to-publish cycle costs ~$0.10–0.25 using `claude-opus-4-6`. Budget $10–20 for solo MVP testing.

### 2. Notion OAuth App

- Go to [notion.so/my-integrations](https://www.notion.so/my-integrations) → New integration
- Set type to **Public** (required — users connect their own workspaces, not just the developer's)
- Add redirect URIs:
  - `http://localhost:3000/api/auth/callback` (local dev via `vercel dev`)
  - `https://<vercel-domain>/api/auth/callback` (add after first Vercel deploy)
- Copy **Client ID** → store as both `NOTION_CLIENT_ID` (server-only) and `VITE_NOTION_CLIENT_ID` (browser-exposed via Vite — intentional, OAuth client IDs are public)
- Copy **Client Secret** → store as `NOTION_CLIENT_SECRET` (server-only, never in bundle)
- **Cost**: Free. Notion API access is included in all Notion plans.
- **Note**: Public integrations require Notion review before use with arbitrary external workspaces. For personal development and testing, it works immediately without review.

### 3. Vercel Account & Project

- Create account at [vercel.com](https://vercel.com) if needed
- Run `vercel link` in the repo root, or import `AlyTomato/inkwell` via the Vercel dashboard
- Set all env vars under Settings → Environment Variables:

| Variable | Source | Server-only? |
|---|---|---|
| `ANTHROPIC_API_KEY` | Anthropic console | Yes |
| `NOTION_CLIENT_SECRET` | Notion integration | Yes |
| `NOTION_CLIENT_ID` | Notion integration | Yes |
| `VITE_NOTION_CLIENT_ID` | Same as `NOTION_CLIENT_ID` | No (Vite bundles it) |

- **Cost**: Free on the Hobby plan. Covers unlimited Edge Function invocations and 100GB bandwidth/month — sufficient for MVP and early beta.

### 4. Local Development

Use `vercel dev` instead of `npm run dev`. This serves both the Vite frontend and the `api/` Edge Functions together on `http://localhost:3000`. Running plain `vite dev` will serve the frontend but all `/api/*` routes will 404.

Copy `.env.local.example` → `.env.local` and fill in credentials before running `vercel dev`.

---

## Complexity Tracking

*No Constitution Check violations. This section intentionally left empty.*
